# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and must never be committed.

# Database
# 【本番 / Railway デプロイ用】Railway ダッシュボード Variables に自動設定される内部 URL:
#   postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway
#
# 【ローカル開発用】以下のどちらかを選ぶ:
#   A) Railway CLI 経由で起動: npm run dev:railway (railway CLI が環境変数を注入)
#   B) Railway ダッシュボード → PostgreSQL → Connect → External URL (TCP Proxy) をコピーして下記に貼る:
#      例: postgresql://postgres:PASSWORD@monorail.proxy.rlwy.net:PORT/railway
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# JWT (本番ではランダムな長い文字列に)
JWT_SECRET="your-jwt-secret-at-least-32-chars"
JWT_COOKIE_NAME="third_age_session"

# Claude API (Anthropic)
# https://console.anthropic.com/ で発行
ANTHROPIC_API_KEY="sk-ant-api03-..."

# 本番のアプリURL（デプロイ後に設定）
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# シード用（初回ユーザー作成。npm run db:seed:railway で本番に admin を作る場合に .env.local に追加）
# SEED_EMAIL="admin@example.com"
# SEED_PASSWORD="your-secure-password"
# SEED_ROLE="admin"
