# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and must never be committed.

# Database (Railway PostgreSQL を追加したら自動で DATABASE_URL が入ります)
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# JWT (本番ではランダムな長い文字列に)
JWT_SECRET="your-jwt-secret-at-least-32-chars"
JWT_COOKIE_NAME="third_age_session"

# Claude API (Anthropic)
# https://console.anthropic.com/ で発行
ANTHROPIC_API_KEY="sk-ant-api03-..."

# 本番のアプリURL（デプロイ後に設定）
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# シード用（初回ユーザー作成。npm run db:seed:railway で本番に admin を作る場合に .env.local に追加）
# SEED_EMAIL="admin@example.com"
# SEED_PASSWORD="your-secure-password"
# SEED_ROLE="admin"
